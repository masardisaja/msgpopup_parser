# msgpopup_parser
WinPopUp IP Messenger (MsgPopUp) log parser tool

This script will parsing MessagePopUp LAN Messenger (MsgPopUp_EN) chat logs into single xlsx file.

Prerequisites:
- xlsxwriter module : pip install XlsxWriter
